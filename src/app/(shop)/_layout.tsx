import { Tabs } from "expo-router";

const TabLayout = () => {
  return <Tabs>
    <Tabs.Screen name="index" options={{}}/>
    <Tabs.Screen  name="orders" options={{}}/>
    <Tabs.Screen />
  </Tabs>;
};
